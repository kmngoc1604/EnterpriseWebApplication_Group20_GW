@model IEnumerable<EnterpriseWebApplication.Models.Entities.Category>

@{
    ViewData["Title"] = "CATEGORY";
    Layout = "_Layout";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">New Category</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Slug)
            </th>
       
            <th></th>
        </tr>
    </thead>
    <tbody>

    @if (Model.Count() > 0)
        @await Component.InvokeAsync("RowTreeCategory", new { categories = Model, level = 0})
    </tbody>
</table>
